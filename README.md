# EcomIQ Sample 4

A standalone, responsive EcomIQ operations concept with a paper-and-ink editorial design. It has no install step or third-party runtime dependencies.

## Run locally

From this folder, run:

```bash
npm run dev
```

Then open http://localhost:8001. This uses port 8001 so it can run alongside the first sample on port 8000.
